<!-- Design: UI tokens, layout rules, and the living component registry -->

# Design

## UI Tokens

Or "Not yet designed" if UI work hasn't started — that is a valid, checkable statement, not a gap to hedge around.

- **Colors:** `[real starting values, e.g. primary #1A1A2E, background #FAFAFA]`
- **Type scale:** `[e.g. 14px / 16px / 20px / 28px — starting scale, revise once real UI exists]`
- **Spacing:** `[e.g. 8px / 16px / 24px / 32px]`

## Layout Rules

Macro (page-level) and micro (component-level) layout patterns actually used in this project.

- `[e.g. Page content max-width 1200px, centered.]`
- `[e.g. Card padding always 16px on mobile, 24px on desktop.]`

## Component Registry

Running list — starts empty. `/moor` writes here.

### `[Component Name]`
File: `[filepath]`

| Property | Class/Value |
| -------- | ----------- |
| Background | |
| Border | |
| Border radius | |
| Text — primary | |
| Text — secondary | |
| Spacing | |
| Hover state | |
| Shadow | |

**Pattern notes:** [why a choice was made, what future components should match]
